import '../../styles/loader.css'

export const Loader = () => {
    return (
        <div className="loader-container" aria-label="loader-identifier">
            <div className="spinner"></div>
        </div>
    );
};